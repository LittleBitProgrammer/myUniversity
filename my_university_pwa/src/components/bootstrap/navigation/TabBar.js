// IMPORT LIB
import React from 'react';

//TODO:// Change in tabbar header

// CREATE A COMPONENT
const TabBar = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
}

export default TabBar;